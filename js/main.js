import React from 'react';//import library from node_modules
import ReactDOM from 'react-dom';

import BootstrapApp from './appBS';

ReactDOM.render(<BootstrapApp />, document.getElementById('body-container'));